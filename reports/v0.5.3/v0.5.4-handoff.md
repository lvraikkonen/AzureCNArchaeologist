# v0.5.3 → v0.5.4 handoff candidate

> Status: `pending_v0.5.3_human_acceptance`
>
> Proposed v0.5.4 theme: SupportArticle mixed/direct-text preservation

This handoff is not yet authoritative. It becomes active only after the user accepts the v0.5.3 Evidence candidate, Machine Gate decision and residual problem map, and the acceptance/version commit passes all gates and receives the local annotated `v0.5.3` tag.

## Proposed first-priority problem

Formal Batch `20260812T125640Z-e5aa4b3f` produced two Core negative bundles:

- `zh-cn/icp-faq`: Evidence ID `3c459fac054bcc50ab6a495b68c64a5913b716d6443e2935f5086398fbbd43ea`;
- `en-us/icp-faq`: Evidence ID `20347d49b0317a0a54f2abc06f35ebc782d65cc4d489003de7fb6e67b5675019`.

Both show that persisted `mainContent` drops direct text after question 18 while L3a passes. v0.5.4 should prove the shared cause and repair SupportArticle mixed-content serialization generically, preserving direct text nodes, element order and meaningful links without product-name or question-number branches.

## Required v0.5.4 evidence

- Freeze a new execution plan before code changes.
- Add real Frozen HTML tests covering direct text before/after `<br/>`, interleaved text/elements, ordering, duplicate text and URL retention across both languages.
- Add a controlled omission/reordering counterexample that L3b must detect.
- Check all SupportArticle families for regressions, not only ICP.
- Run a new clean-commit Batch and generate new current L3a/L3b Evidence for affected representative items; preserve the v0.5.3 failed bundles unchanged as historical negative evidence.
- Run the full Batch and explain every status/error difference from `20260812T125640Z-e5aa4b3f`.

## Boundaries carried forward

- v0.5.3 Machine Gate remains `parallel_only` and `runtime_effective=false`; v0.5.4 does not inherit activation authority.
- `en-us/icp-faq` still cannot prove translation or localization quality while it binds the Chinese Source snapshot.
- `en-us/time-series-insights` remains unqualified until its exact soft-category reconstruction boundary is resolved or explicitly re-owned.
- No historical Evidence, Batch, accepted report or tag may be overwritten.

If accepted, later order is v0.5.5 Simple page-global boundary/classification, v0.5.6 filter-control truth split and safe fixes, then v0.6 remaining structural/config groups plus CMS staging round-trip checks. Full details and affected items are in `reports/v0.5.3/residual-problem-map.md`.
