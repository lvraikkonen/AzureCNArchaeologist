# v0.5.4 → v0.5.5 handoff candidate

> Status: `pending_v0.5.4_human_acceptance`
>
> Proposed v0.5.5 theme: Simple page-global boundary and strategy classification

This handoff is not yet authoritative. It becomes active only after the user accepts the v0.5.4 Batch, Evidence identities/paths, regression/comparison reports and claim limitation, and after the acceptance/version commit passes the final gates and receives the local annotated `v0.5.4` tag.

## v0.5.4 candidate outcome carried forward

- Clean producer: `658987d9ef221aeb29743bb3832a2aee064584b9`.
- Formal Batch: `20260813T013534Z-b9e91703`.
- New bilingual ICP Evidence is 1/1 passed for each language; the exact Source direct text is restored once in physical order.
- New `zh-cn/sla-sql-data` witness Evidence is 1/1 passed.
- The v0.5.3 bilingual ICP failed bundles remain immutable proof of the old producer behavior.
- All 197 unaffected SupportArticle payloads are byte-identical at fixed inputs; all 199 executable SupportArticle items succeed and validate in the formal Batch.
- Full Batch membership/status/error is unchanged; only the two explained `mainContent` payloads differ.
- `en-us/icp-faq` still proves fidelity to its Chinese Source binding, not English translation or localization quality.
- Machine Gate remains `parallel_only` with `runtime_effective=false`.

## Frozen v0.5.5 problem group

v0.5.5 remains R2 from the accepted residual problem map: Simple page-global boundary and classification for the current 16 fail-closed items, both languages of:

- `azure-defender`;
- `batch`;
- `bot-services`;
- `core-control-plane`;
- `firewall-manager`;
- `frontdoor`;
- `service-fabric`;
- `virtual-network`.

The current signatures include intrinsic boundary failures, active-filter/simple classification conflicts, exact FAQ/SLA boundary gaps and non-direct-child boundary failures. They are a problem family to classify and split, not a pre-proven single root.

## Required v0.5.5 planning discipline

- Start with a read-only structure/ownership preflight and freeze a separate execution plan before implementation.
- Derive page-global boundaries from intrinsic DOM evidence or explicit Product Definition declarations; do not fall back to `.pure-content` or `body` as a guessed boundary.
- Separate genuine static pages from pages with active controls before changing strategy classification.
- Retain fail-closed behavior when ownership or boundary cannot be proved.
- Test boundary-too-wide, boundary-too-narrow, adjacent-component capture and active-control counterexamples.
- Compare a new clean-commit Batch against `20260813T013534Z-b9e91703` and explain every status/error/payload delta.
- Do not reuse v0.5.4's SupportArticle repair as authority for Simple semantics.

## Boundaries carried forward

- L3a and L3b remain parallel claims; no runtime Machine Gate activation authority is inherited.
- Historical Batches, Evidence, reports and tags remain immutable.
- `en-us/time-series-insights` remains `not_qualified` and owned by R5/v0.6; no bundle may be fabricated.
- v0.5.6 remains the filter-control truth split and safe-fix phase. R4-R6 remain assigned to v0.6.
- The architecture review candidates recorded in `reports/v0.5.4/architecture-preflight.md` are not a license for opportunistic `v053_*` renaming or compatibility wrappers in v0.5.5. The full multi-Agent v0.5 code review remains scheduled after v0.5.6 acceptance/tag and before the v0.6 Plan freeze.

No Release, upload, publication, push, merge, PR or L4 Review Decision is part of this handoff.
