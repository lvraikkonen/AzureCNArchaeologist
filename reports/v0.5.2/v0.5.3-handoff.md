# v0.5.2 → v0.5.3 交接候选

- 生成时间：2026-08-12T04:09:11Z
- 当前分支：`codex/v0.5-development`
- v0.5.2 当前状态：**formal item 与自动门禁通过，等待人工逐状态复核和 exact Evidence identities 接受**
- verifier implementation commit：`873eeac3ddd7070d468c64926c1ff8f6962b5395`
- formal target：`20260811T171630Z-e80afabe` / `zh-cn/api-management`

本文件是 acceptance candidate 的交接材料，不宣告 v0.5.2 已接受。只有人工复核 `review.html` 的全部五个状态并明确接受 Evidence semantic SHA、projection SHA 和 candidate commit 后，才可生成最终 acceptance/version/tag commit。

## 1. 已形成的正式候选

首个 formal L3b bundle 位于：

`runs/20260811T171630Z-e80afabe/independent-fidelity/zh-cn/pricing/api-management/`

| 身份 | 值 |
|---|---|
| L3b verdict / coverage | `passed` / 5 required、5 completed、5 passed、0 failed、0 blocked |
| Evidence semantic SHA | `86915841ea662a63d88ded69e385f3f98548ebcc1228b84219fbfe88d5b72c2c` |
| Evidence artifact SHA | `83ac34e78a4c7abd5025fe5fdb02a5076a021721b719bd00dd76adaf663e2770` |
| Projection SHA | `192783dfa00a6f32ee6fdf463544bc6bad3cf4c638362f5ab5f9183dede2d3a6` |
| `review.html` SHA | `95eec9665b7e22d3a202ac2ada30cb38a6fabd6fe7ff480b072b75f0d97d9500` |
| Reconstruction Basis semantic SHA | `df43667829458078e9fecfa2ad59e601d91b19f0ff82fe4c72a3ad40e15f9537` |

`record` 与立即执行的 deterministic `verify` 均返回相同 identities。record 前后完整 Batch inventory 为 `2185 → 2207`；唯一新增 22 个 regular files 全部位于 canonical prefix，其他路径没有新增、删除或字节变化。

## 2. v0.5.2 提供给下一阶段的能力

- exact-target binder：绑定 immutable input manifest、current batch manifest revision 1483、current payload、Source、Product Definition、soft-category、L3a validation/sampling artifacts 和最小 profile；任一身份漂移均 fail-closed。
- `api-management` 独立重建：desktop-authoritative、default-first 五状态，mobile 仅校验 machine domain/target；mobile default marker 不参与状态选择。
- 精确 ownership：配置 key 为 `os`；表集合必须形成确定分区；duplicate exact config row、缺失/重复 DOM table ID、多表 wrapper 或嵌套 ownership 均 blocked。
- 四层比较：raw bytes、DOM structure、tag sequence、visible text；任何一层 mismatch 都使对应状态 failed。
- 不可变 recorder：clean implementation commit 门禁、scope/qualification/result matrix、same-filesystem 临时构建与原子晋升、no-overwrite、closed-world bundle 与 deterministic replay。
- inert review projection：raw fragment 使用 `.html.txt`，严格 CSP，只允许 same-document anchors；semantic identity 与 projection identity 分离。
- 独立性与反证：静态 import firewall、runtime sentinel、真实 common-mode 状态交换（共享 replay 可过而 L3b 失败）、路径/symlink/JSON/CSP/dirty-tree/失败与 blocked bundle 测试。

## 3. v0.5.3 必须冻结的 Core 8

v0.5.3 应为四类页面各选一个 Core 产品，并覆盖两种语言，共 8 个 item：

1. `zh-cn/api-management`、`en-us/api-management`（`region_filter`）；
2. `zh-cn/cloud-services`、`en-us/cloud-services`（`complex`）；
3. `zh-cn/service-bus`、`en-us/service-bus`（`simple_static`）；
4. `zh-cn/icp-faq`、`en-us/icp-faq`（`support_article`）。

v0.5.3 Execution Plan 必须明确当前 `zh-cn/api-management` bundle 是直接复用、deterministic replay，还是在新的 Batch/current binding 上重新生成；不得让实现隐式决定。随后才可冻结四类页面 profile successor、Workbench 入口和基于真实覆盖率/失败分布/人工复核结果的 Machine Gate policy。

## 4. 分母外 carry-over qualification

以下两项不进入 Core 8 分母，但必须在 v0.5.3 单列给出 `passed`、`failed`、`blocked` 或 `not_qualified` 结论与后续 owner：

- `en-us/time-series-insights`
- `zh-cn/sla-sql-data`

v0.5.3 还必须核对生产抽取/L3a 是否采用与本次 L3b 相同的“唯一配置行内 duplicate table ID ordered-unique successor”政策。若两条 machine lane 不同，必须通过后续 ADR/profile 决策显式对齐。

## 5. 已知边界与未决事项

- v0.5.2 只正式覆盖 `zh-cn/api-management`；没有给其他语言、产品或页面族赋予 L3b 结论。
- 当前正式 profile 仍为 `v0.5.1-independent-fidelity-minimal`；本次真实运行没有证明必须创建 Profile/Basis/Evidence successor，v0.5.3 应按四类页面的实际缺口再决定。
- 本次真实配置没有 Configuration Hygiene warning；duplicate-ID warning 的 ordered-unique、投影和 console 行为由定向测试证明。
- 没有激活 Pipeline Machine Gate，没有接入现有 Workbench，没有写 L4 Review Decision 或 `manual_l3b_*` 状态。
- 没有修改 Review、Release、upload 或 Approval Eligibility policy；没有 build Release、upload、push、merge 或创建 PR。
- reference Batch 仍保留其已解释的全局失败；本单项 L3b passed 不代表整个 Batch green。
- 在人工复核与 exact identity 接受前，项目版本仍为 `0.5.1`，不得创建 `v0.5.2` tag。

机器可读的候选状态和完整 inventory delta 见 `reports/v0.5.2/acceptance-status.json`；人工复核清单见 `reports/v0.5.2/acceptance-status.md`。
